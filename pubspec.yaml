name: hawk_fab_menu
description: A customizable floating action button menu that will pop up small fabs on top
version: 1.2.1
homepage: https://github.com/arnold-parge/hawk_fab_menu

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
